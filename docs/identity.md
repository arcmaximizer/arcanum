# Identity

The app namespaces `arc`, `arcanum`, `sys`, `dev`, `official`, `system` and
`local` are reserved.

Each Arcanum gets its own username, such as `arcanum`. This is tied to a
cryptographic keypair and tracked on the Arcnet.